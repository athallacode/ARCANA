/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  devIndicators: {
    buildActivity: false,
    staticPageIndicator: false,
  },
}

module.exports = nextConfig;
